import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    defaultStyling: {
        width : '100%',
        alignItems : 'center',
        paddingBottom : 30
    }
})

export default styles